clone the repo 
install node packages at the location project cloned
go to directory , use the command below 
-> npm install
-> to view in tests in runner npx cypress open
-> run tests in headless  ->  npm test 
reports are generated at location 
->cypress/reports/html

->cypress/reports/videos
